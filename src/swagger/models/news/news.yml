tags:
  - news

summary: Get news

parameters:
  - in: query
    name: Authorization
    schema:
      type: string
      example: write_here
    required: true
    description: Authorization to run endpoint

responses:
  200:
    description: OK
  400:
    description: BAD REQUEST
  500:
    description: INTERNAL SERVER ERROR