tags:
  - query

summary: Get all queries in queue

parameters:
  - in: query
    name: Authorization
    schema:
      type: string
      example: digite_aqui
    required: true
    description: Authorization to run endpoint

responses:
  200:
    description: OK
  400:
    description: BAD REQUEST
  500:
    description: INTERNAL SERVER ERROR